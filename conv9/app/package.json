{
  "name": "conv9-listener-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:browser",
    "test:all": "npm run test:browser && npm run test:native-audio",
    "test:browser": "node tests/e2e.mjs",
    "test:native-audio": "cargo build --manifest-path src-tauri/Cargo.toml && node tests/native-audio.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0"
  }
}
